# Django
DJANGO_SECRET_KEY=cambiar-esta-clave-secreta-en-produccion
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=config.settings.local
# Orígenes permitidos para el frontend (separados por coma). En producción, el dominio real.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Database
POSTGRES_DB=dimedservice
POSTGRES_USER=dimed
POSTGRES_PASSWORD=dimed_dev_2026
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis (broker/resultados de Celery y caché)
REDIS_URL=redis://redis:6379/0
REDIS_CACHE_URL=redis://redis:6379/1

# MinIO (S3 compatible)
MINIO_ROOT_USER=dimedminio
MINIO_ROOT_PASSWORD=dimedminio2026
MINIO_ENDPOINT=minio:9000
MINIO_BUCKET_NAME=dimedservice
MINIO_USE_SSL=False

# Email (SendGrid)
SENDGRID_API_KEY=
DEFAULT_FROM_EMAIL=servicio@dimedhealthcare.com

# Claude API (para motor de plantillas IA)
ANTHROPIC_API_KEY=

# WhatsApp Business API
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=

# Timezone
TZ=America/Guayaquil
